#include "main.h"

/**
 * _abs -Calculates the absolute value of  number.
 *
 * @n: The integer to be computed.
 *
 * Return: The absolute value of the integer.
 *
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
